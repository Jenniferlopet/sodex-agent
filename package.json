{
  "name": "sodex-agent-fresh",
  "version": "1.0.0",
  "private": true,
  "description": "Static Netlify SoDEX AI agent dashboard with serverless API fallback.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "2.8.2"
  },
  "devDependencies": {}
}
